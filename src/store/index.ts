import Vue from 'vue'
import Vuex from 'vuex'
// Modules imports
import Clients from '@/store/modules/clients'

Vue.use(Vuex)

export default new Vuex.Store({
	state: {},
	mutations: {},
	actions: {},
	modules: { Clients },
})
